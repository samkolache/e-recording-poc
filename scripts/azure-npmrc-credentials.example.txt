# Paste these lines into your USER-LEVEL .npmrc (~/.npmrc)
# Replace YOUR_BASE64_PAT with the output of: node -e "require('readline').createInterface({input:process.stdin,output:process.stdout,historySize:0}).question('PAT> ',p => { console.log(Buffer.from(p.trim()).toString('base64')); process.exit(); })"
#
# Project-scoped feed requires BOTH blocks (registry + /npm/) per Microsoft docs.

; begin auth token (Azure DevOps - Exos.DesignSystem)
//pkgs.dev.azure.com/ServiceLink/Architecture/_packaging/Exos.DesignSystem/npm/registry/:username=ServiceLink
//pkgs.dev.azure.com/ServiceLink/Architecture/_packaging/Exos.DesignSystem/npm/registry/:_password=YOUR_BASE64_PAT
//pkgs.dev.azure.com/ServiceLink/Architecture/_packaging/Exos.DesignSystem/npm/registry/:email=npm requires email to be set but doesn't use the value
//pkgs.dev.azure.com/ServiceLink/Architecture/_packaging/Exos.DesignSystem/npm/:username=ServiceLink
//pkgs.dev.azure.com/ServiceLink/Architecture/_packaging/Exos.DesignSystem/npm/:_password=YOUR_BASE64_PAT
//pkgs.dev.azure.com/ServiceLink/Architecture/_packaging/Exos.DesignSystem/npm/:email=npm requires email to be set but doesn't use the value
; end auth token
